// Import libraries for making component
import React from 'react';
import {Text} from 'react-native';

// Make component
const Header = () => {
  return <Text>Hello</Text>;
};

// Make the component available for other part of the app.
export default Header;
